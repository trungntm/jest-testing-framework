const sum = (a, b)  => a + b
const sub = (a, b) => a - b
function compileAndroidCode() {
    throw new ConfigError('you are using the wrong JDK');
  }
module.exports = { sum, sub, compileAndroidCode }
// export default { sum, sub }